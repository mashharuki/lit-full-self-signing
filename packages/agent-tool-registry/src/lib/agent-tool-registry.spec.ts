import { agentToolRegistry } from './agent-tool-registry';

describe('agentToolRegistry', () => {
  it('should work', () => {
    expect(agentToolRegistry()).toEqual('agent-tool-registry');
  });
});
